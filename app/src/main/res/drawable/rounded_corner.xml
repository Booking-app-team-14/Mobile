<!-- res/drawable/rounded_corner.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>  <!-- Boja pozadine -->
    <corners android:radius="20dp"/>  <!-- Zaobljeni uglovi -->
    <stroke android:width="2dp" android:color="@color/purple_200"/>  <!-- Ljubišasti okvir -->
</shape>
